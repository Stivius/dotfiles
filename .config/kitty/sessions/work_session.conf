new_tab Tasks
layout grid
cd ~
launch bash -i -c "todo.sh edit && bash"
launch bash -i -c "watch_dir .todo/ './.todo/work_focus' && bash"

new_tab Work
cd ~/playson
layout stack
launch
launch

new_tab Work-Tests
cd ~/playson
layout grid
launch
launch
launch

new_tab Scratchpad
cd ~
layout stack
launch bash -i -c 'vim ./scratchpad && bash'
